CREATE TABLE [dbo].[Orders] (
    [ProdName] VARCHAR(50) NOT NULL,
    [Price]    INT            NOT NULL,
    [Qty]      INT            NOT NULL,
    [Date]     VARCHAR (50)   NOT NULL,
    [Total]    INT            NOT NULL
);

